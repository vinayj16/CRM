# Ignore build outputs
bin/
obj/
publish/

# Visual Studio / IDE
.vs/
.vscode/
.idea/

# User-specific files
*.user
*.suo
*.userosscache
*.sln.docstates

# Logs
*.log

# Node
node_modules/

# Secrets and credentials
firebase-credentials.json
realestatecrm-*-firebase-adminsdk-*.json
*firebase-adminsdk*.json
credentials.json
secrets/
secrets.json

# App settings — intentionally committed so deployments have a working baseline.
# Override secrets in production with Railway Variables (e.g. MongoDb__ConnectionString).
# Do NOT commit firebase-credentials.json (below) or any service-account keys.

# Environment files
.env
.env.*
.env.local

# Certificate and key files
*.key
*.pem
*.p12
*.pfx
*.jks
*.p8

# Azure / Publish
PublishProfiles/
*.Publish.xml
*.publishsettings

# Data exports, runtime uploads & debug artifacts (not source code)
All_MongoDB_Data.json
PayoutDebug.txt
wwwroot/uploads/
*.vsix
_v3.html
