// Minimal vendor stubs
(function() {
  if (typeof window !== 'undefined') {
    // AnchorJS stub
    window.anchors = window.anchors || { add: function() {} };
    
    // is.js stub
    window.is = window.is || {};
    
    // lodash stub
    window._ = window._ || {};
    
    // list.js stub
    window.List = window.List || function() {};
    
    // dayjs stub
    window.dayjs = window.dayjs || function(d) { return d; };
    
    // glightbox stub
    window.GLightbox = window.GLightbox || function() { return { init: function() {} }; };
  }
})();
